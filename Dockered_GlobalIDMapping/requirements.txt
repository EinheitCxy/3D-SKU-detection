# 3D SKU Detection System Core Dependencies
# Aligned with code/pyproject.toml

fastapi
uvicorn[standard]

# Core scientific computing
numpy>=1.21.0
scipy>=1.10.1
pandas>=1.5.0
bson
numpy

# Machine learning frameworks
torch>=2.0.0
torchvision>=0.15.0
scikit-learn>=1.3.2

# Computer vision
opencv-python>=4.8.0
Pillow>=9.0.0

# 3D processing and visualization
open3d>=0.19.0
trimesh>=4.0.0
matplotlib>=3.7.5
viser>=0.2.0

# 3D file formats
pygltflib>=1.15.0

# Deep learning utilities
einops>=0.6.0

# Utilities
colorlog>=6.9.0
rich>=13.0.0
pyyaml>=6.0.2
huggingface-hub>=0.34.2
safetensors>=0.5.3

# Optional: Interactive demos (install separately if needed)
# gradio>=4.0.0

# Optional: VGGT model support (install separately if needed)
# huggingface_hub>=0.22.0
# roma>=1.3.0

# Optional: CPU acceleration (recommended, supports Python 3.8-3.12)
faiss-cpu>=1.7.0

# Optional: GPU acceleration (requires CUDA, Python 3.8-3.10 only)
# faiss-gpu>=1.7.0,<1.8.0
# cupy-cuda12x>=12.0.0 